<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- NOTE: EditTextPreference accepts EditText attributes. -->
    <!-- NOTE: EditTextPreference's summary should be set to its value by the activity code. -->

    <!-- NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog to
         dismiss it. -->
    <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->

    <ListPreference
        android:defaultValue="2"
        android:entries="@array/pref_item_per_row"
        android:entryValues="@array/pref_item_per_row_values"
        android:key="items_per_row_list"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null"
        android:title="@string/items_per_row" />

    <ListPreference
        android:defaultValue="1"
        android:entries="@array/pref_sortway"
        android:entryValues="@array/pref_sortway_values"
        android:key="pref_sortway_list"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null"
        android:title="@string/sortway" />
</PreferenceScreen>
